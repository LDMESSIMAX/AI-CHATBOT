name: my_flutter_project
description: A new Flutter project.

publish_to: 'none' # Remove this if you plan to publish to pub.dev.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3   
  # Add your other dependencies like the http package here:
  # http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter_icons:
  android: true
  ios: true
  image_path: "C:\Users\Harish M\sentia-backend\logo.jpg"
flutter:
  uses-material-design: true

